'use strict';

var {StyleSheet} = require('react-native');

var styles = StyleSheet.create({
  viewLeft : {
    margin: 10,
    fontSize: 15,
    textAlign: 'left',
  },
  viewRight : {
    margin: 10,
    fontSize: 15,
    textAlign: 'right',
  },
});

module.exports = styles;
